# 12) gdb

gdb는 GNU debugger로서 디버깅을 하는 도구입니다.

gdb를 사용하려면 컴파일을 할때 파일에 디버깅 정보를 포함하도록 옵션을 지정해주어야합니다. 

### gdb를 위한 컴파일 방법

```bash
$ gcc -g 소스코드
```

### gdb 실행

```bash
$ gdb 실행파일
```

gdb의 대상은 소스코드가 아닌 **실행 파일**임을 명심해야합니다.